class ArrayUtil {
    public static int [] getSeqArray(int n, int initial)
    {
        int [] a = new int[n];

        for (int i = 0; i < n; ++i)
            a[i] = initial++;

        return a;
    }
